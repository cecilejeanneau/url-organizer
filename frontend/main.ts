// Bootstrap is handled inline in index.html via <script type="module">
// which imports Alpine from CDN ESM and registers the store.
export { createUrlOrganizerStore } from './store/urlOrganizerStore.js';
